# Both default to http://localhost:4000 if unset, which is correct for
# local dev when the API runs on its default port - you usually don't need
# this file at all locally. Only needed if the API runs somewhere else.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
